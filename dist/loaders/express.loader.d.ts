import { INestApplication } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
declare const _default: (app: INestApplication, configService: ConfigService) => Promise<void>;
export default _default;
